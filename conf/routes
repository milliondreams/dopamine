# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET     /                           com.tuplejump.playYeoman.Yeoman.redirectRoot(base="/ui/")

GET     /ui                         com.tuplejump.playYeoman.Yeoman.index

->	    /ui/                         yeoman.Routes

GET      /db                         controllers.Application.dbSocket(host:List[String], port:Option[Int],  username:Option[String], password:Option[String])
